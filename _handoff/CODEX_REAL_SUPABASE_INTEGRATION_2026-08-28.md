# تحقق وربط Supabase الحقيقي - 2026-08-28

## المشروع الحي

- Project ref: `szhpqyvxodhaichrrdfb`.
- نجحت نقاط Auth وStorage وPostgREST الحية بحالة HTTP 200.
- نجح مفتاح anon في قراءة الكتالوج العام، ونجح service role في الفحص الإداري.
- وُحدت ملفات `.env.local` الخاصة بـ web/admin/helper/partner/mobile على المشروع نفسه.
- لم يُنقل service role إلى الموبايل أو أي متغير `NEXT_PUBLIC_*`.

## النسخة الاحتياطية

- أُخذ dump للمخطط والبيانات قبل أي تعديل بعيد.
- المسار: `_backups/supabase-20260828-204643/` وهو مستثنى من Git.
- يحتوي `schema.sql` و`data.sql`.
- أظهر pg_dump تحذير ترتيب استعادة لعلاقتين دائريتين؛ اكتمل الملفان بنجاح.

## الهجرات والفحص

- تطابقت جميع الهجرات المحلية والبعيدة حتى `20260805100000`.
- أضيفت واختُبرت محلياً هجرة `20260828210000_remote_least_privilege_hardening.sql`.
- أظهر dry-run أن هذه الهجرة وحدها ستُطبق، ثم طُبقت على المشروع الحي.
- السجل البعيد النهائي: 75 هجرة، وآخرها `20260828210000`.
- نجح `supabase db lint` محلياً وعلى القاعدة الحية: `No schema errors found`.

## نتائج الأمان الحية

- جداول public بلا RLS: 0.
- سياسات RLS في public عند الجرد: 64.
- دوال SECURITY DEFINER المتاحة لـ PUBLIC بعد الإصلاح: 0.
- منح INSERT/UPDATE/DELETE الزائدة للأدوار العامة على الجداول الحساسة المفحوصة: 0.
- اختبار anon للكتابة في `admin_profiles`: مرفوض HTTP 401.
- اختبار anon للدالة الداخلية القديمة: مرفوض HTTP 401.
- لم يترك الاختبار أي سجل.

## التخزين والبيانات

- buckets: ستة.
- العام: `product-images`, `product-videos` فقط.
- الخاص: `exchange-images`, `exchange-qr-codes`, `loyalty-qr-codes`, `product-request-images`.
- البيانات الحالية: 9 منتجات منشورة، 21 variant فعالاً في المخزون، 12 فئة، 14 سعر شحن، 5 أقسام للصفحة الرئيسية، و15 إعداد نظام.
- Auth users: صفر. لا يوجد مدير أو عميل أو هيلبر أو شريك فعلي بعد.

## تحقق الكود بعد الربط

- Vitest: 48/48 ناجحة.
- TypeScript: web/admin/helper/partner/mobile/shared/database/ui/adapters ناجحة.
- ESLint: صفر أخطاء؛ التحذيرات الحالية أسلوبية.
- Expo Doctor: 17/17.
- تطبيق الموبايل يستخدم Bearer مع Supabase الحقيقي، وSecureStore للجلسة ورمز push، وسلة خادم وcheckout ذري، وحساب/عناوين/ولاء/استبدال/فواتير وإشعارات مرتبطة بالـ APIs نفسها.
- فُحص الويب محلياً ضد Supabase الحقيقي: ظهرت المنتجات الحية، وفتحت نافذة Login/Register من الصفحة نفسها بلا أخطاء Console.
- فُحصت النافذة على سطح المكتب وقياس 390x844؛ لا تداخل أو تمرير أفقي.
- أزيلت روابط placeholder الخارجية والنصوص العربية التالفة من واجهات الموبايل المفحوصة.
- لم يُشغّل أي build.

لم تضف جولة إكمال الموبايل أي هجرة أو كتابة مباشرة إلى قاعدة الإنتاج؛ بقي سجل المشروع الحي عند 75 هجرة، واستُخدمت العقود المنشورة القائمة.

## متبقٍ يتطلب مالك المشروع أو بيانات تشغيل

1. Supabase CLI مسجل حالياً بحساب لا يملك امتياز إدارة هذا المشروع. اتصال DB وREST يعمل، لكن `supabase link` الرسمي يحتاج تسجيل الدخول بحساب المالك.
2. لا توجد نطاقات إنتاج؛ عناوين التطبيقات الحالية localhost.
3. Auth email فعال والتحقق مطلوب، لكن Google OAuth معطل ولا يوجد SMTP إنتاج مثبت.
4. لا يوجد أول مستخدم مدير، وبالتالي لم يُختبر TOTP بدور حقيقي.
5. Resend وExpo notification dispatch وcron production غير مضبوطة بأسرار منصة النشر.
6. الدفع مضبوط على COD فقط؛ Sham Cash يفشل مغلقاً حتى توفير عقد المزود الحقيقي.
7. تحذيرا `image-size` بلا إصدار مصحح متاح، وهما محصوران في Metro الخاص بتجهيز الموبايل وليسا في تطبيقات Next.js أو runtime المنشور.
8. يلزم اختبار staging بأدوار فعلية ودورة طلب/إلغاء/ولاء/استبدال كاملة قبل الإعلان العام.
